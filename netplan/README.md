Change the IP address of enP7s7 to your local network

Uninstall Network Manager

sudo rm /etc/netplan/*

sudo cp 01-spark-spark1.yaml -> spark1 /etc/netplan
sudo 600 /etc/netplan/*
sudo netplan apply

